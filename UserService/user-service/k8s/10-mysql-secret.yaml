# Replace values before real clusters; prefer: kubectl create secret generic mysql-secret -n user-service --from-literal=...
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: user-service
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: changeme
